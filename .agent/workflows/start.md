---
description: Start the Battery Monitor
---

1. Ensure dependencies are installed: `pip install psutil plyer pystray Pillow`
// turbo
2. Start the monitor script:
`python battery_monitor.py --threshold 100`
